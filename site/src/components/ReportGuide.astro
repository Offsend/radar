<details class="card report-guide">
	<summary>How to read this report</summary>
	<div class="report-guide-body">
		<p class="report-guide-lead">These are not confirmed leaks.</p>
		<p>
			Radar uses path-level checks to detect files and folders that may become visible to AI coding
			tools if they are not covered by AI-specific ignore rules.
		</p>
		<p>
			A <strong>risk signal</strong> is a file committed to the repository whose path matches a
			sensitive category (for example <code>.env</code> or <code>*.pem</code>). Counts are file
			matches at the path level — Radar never opens the files.
		</p>
		<ul class="report-guide-points">
			<li>No file contents were read.</li>
			<li>No secrets were collected.</li>
			<li>No repository names are published without opt-in.</li>
			<li>
				Repositories are a manually curated cohort, not a random sample — read these as directional
				signals, not population-level statistics.
			</li>
		</ul>
	</div>
</details>

<style>
	.report-guide summary {
		cursor: pointer;
		font-family: var(--heading);
		font-size: 1.15rem;
		font-weight: 700;
		color: var(--text);
		list-style: none;
		border-radius: var(--r-sm);
		transition: color 0.15s ease;
	}

	.report-guide summary:hover {
		color: var(--blue-text);
	}

	.report-guide summary::-webkit-details-marker {
		display: none;
	}

	.report-guide summary::before {
		content: '▸ ';
		display: inline-block;
		transition: transform 0.15s ease;
	}

	.report-guide[open] summary::before {
		transform: rotate(90deg);
	}

	.report-guide-body {
		margin-top: 0.75rem;
	}

	.report-guide-lead {
		margin: 0 0 0.5rem;
		font-weight: 600;
		color: var(--text);
	}

	.report-guide p {
		margin: 0;
		font-size: 0.95rem;
		color: var(--text-sub);
		line-height: 1.5;
	}

	.report-guide :global(code) {
		font-family: var(--mono);
		font-size: 0.88em;
		background: var(--bg-2);
		padding: 0.1em 0.35em;
		border-radius: 4px;
	}

	.report-guide-points {
		margin: 0.75rem 0 0;
		padding-left: 1.15rem;
		font-size: 0.95rem;
		color: var(--text-sub);
		line-height: 1.5;
	}

	.report-guide-points li + li {
		margin-top: 0.25rem;
	}
</style>
